```c++
int find1(int l, int r) //找第⼀个⼩于等于x的数
{
    while (l < r)
    {
        int mid = l + r + 1 >> 1;
        if (a[mid] <= x)
            l = mid;
        else
            r = mid - 1;
    }
    return l;
}
int find2(int l, int r) //找第⼀个⼤于等于x的数
{
    while (l < r)
    {
        int mid = l + r >> 1;
        if (a[mid] >= x)
            r = mid;
        else
            l = mid + 1;
    }
    return l;
}
```

