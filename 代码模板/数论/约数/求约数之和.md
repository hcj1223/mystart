# 求约数之和

```c++
auto primes = getdivide(n);
long long res = 1;
for (auto item : primes)
{
    int p = item.first, a = item.second;
    long long t = 1;
    while (a--)
        t = (t * p + 1) % mod;
    res = res * t % mod;
}
```

