# 分解质因数

```c++
void divide(int n)
{
    for (int i = 2; i <= n / i; i++) // x最多只有一个大于sqrt(x)的质因子
    {
        if (n % i == 0)
        {
            int cnt = 0; //记录因子i的个数
            while (n % i == 0)
                n /= i, s++;
        }
    }
    if (n > 1) // 说明剩下一个大于sqrt(x)的质因子
}
```
