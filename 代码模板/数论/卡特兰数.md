# 卡特兰数

```c++
int catalan(int n)
{
    int a = 2 * n, b = n;
    int res = 1;
    for (int i = 1, j = a; i <= b; i++, j--)
    {
        res = (ll)res * j % p;
        res = (ll)res * qmi(i, p - 2) % p;
    }
    res = (ll)res * qmi(n + 1, p - 2) % p;
    return res;
}
```

