# 链表逆序

```c++
void reverselist(list &t)
{
    list p = t->next;
    t->next = NULL;
    while (p)
    {
        list q = p;
        p = p->next;
        q->next = t->next;
        t->next = q;
    }
}
```

