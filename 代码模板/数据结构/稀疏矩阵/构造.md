# 构造

```c++
int n, m;
cin >> n >> m;
vector<pll> a;
for (int i = 0; i < n; i++)
    for (int j = 0; j < m; j++)
    {
        int x;
        cin >> x;
        if (x)
            a.push_back({{i, j}, x});
    }
```

