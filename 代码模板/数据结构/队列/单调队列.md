# 单调队列

```c++
deque<int> dq;
dq.push_back(0);
for (int i = 1; i <= n; i++)
{
    while (dq.size() && dq.back() < i - m)
        dq.pop_front();
    while (dq.size() && a[dq.front()] >= a[i])
        dq.pop_back();
    dq.push_back(i);
}
```